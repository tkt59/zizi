-- Zizi Panel v2.1 | Redesign UI
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local RootPart = Character:WaitForChild("HumanoidRootPart")

local KEYBIND = Enum.KeyCode.P

local State = {
    Speed = 16, FlySpeed = 50, JumpPower = 50,
    Noclip = false, Fly = false, InfiniteJump = false, AntiVoid = false,
    NoFog = false, Fullbright = false, NoShadows = false, TimeOfDay = 12,
    ESPEnabled = false, Chams = false, NameTags = false, HealthBar = false,
    ESPRange = 500, SpamChat = false, FakeAdmin = false,
}

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ZiziPanel"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
if gethui then ScreenGui.Parent = gethui()
elseif game:GetService("CoreGui") then pcall(function() ScreenGui.Parent = game:GetService("CoreGui") end) end
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end

local C = {
    BG        = Color3.fromRGB(12, 12, 18),
    BG2       = Color3.fromRGB(18, 18, 28),
    Surface   = Color3.fromRGB(28, 28, 42),
    Border    = Color3.fromRGB(60, 60, 100),
    Accent    = Color3.fromRGB(120, 80, 255),
    AccentB   = Color3.fromRGB(80, 180, 255),
    White     = Color3.fromRGB(230, 230, 255),
    Muted     = Color3.fromRGB(110, 110, 150),
    Red       = Color3.fromRGB(220, 60, 80),
    Green     = Color3.fromRGB(60, 200, 120),
    ON        = Color3.fromRGB(120, 80, 255),
    OFF       = Color3.fromRGB(40, 40, 60),
}

-- MAIN FRAME
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 420, 0, 520)
Main.Position = UDim2.new(0.5, -210, 0.5, -260)
Main.BackgroundColor3 = C.BG
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Main.Visible = false
Main.Parent = ScreenGui

local MainCorner = Instance.new("UICorner", Main)
MainCorner.CornerRadius = UDim.new(0, 16)

-- Bordure lumineuse
local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = C.Accent
Stroke.Thickness = 1.5
Stroke.Transparency = 0.5

-- Fond dégradé simulé via un frame overlay
local GradOverlay = Instance.new("Frame")
GradOverlay.Size = UDim2.new(1, 0, 0.4, 0)
GradOverlay.BackgroundColor3 = C.Accent
GradOverlay.BackgroundTransparency = 0.92
GradOverlay.BorderSizePixel = 0
GradOverlay.ZIndex = 0
GradOverlay.Parent = Main
Instance.new("UICorner", GradOverlay).CornerRadius = UDim.new(0, 16)

-- TITLEBAR
local Titlebar = Instance.new("Frame")
Titlebar.Size = UDim2.new(1, 0, 0, 44)
Titlebar.BackgroundColor3 = C.BG2
Titlebar.BorderSizePixel = 0
Titlebar.ZIndex = 2
Titlebar.Parent = Main
Instance.new("UICorner", Titlebar).CornerRadius = UDim.new(0, 16)

-- Fix coin bas de titlebar
local TitleFix = Instance.new("Frame")
TitleFix.Size = UDim2.new(1, 0, 0.5, 0)
TitleFix.Position = UDim2.new(0, 0, 0.5, 0)
TitleFix.BackgroundColor3 = C.BG2
TitleFix.BorderSizePixel = 0
TitleFix.ZIndex = 2
TitleFix.Parent = Titlebar

-- Pastilles macOS
for i, col in ipairs({Color3.fromRGB(255,95,87), Color3.fromRGB(254,188,46), Color3.fromRGB(40,200,64)}) do
    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0, 11, 0, 11)
    dot.Position = UDim2.new(0, 10 + (i-1)*18, 0.5, -5)
    dot.BackgroundColor3 = col
    dot.BorderSizePixel = 0
    dot.ZIndex = 3
    dot.Parent = Titlebar
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
end

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Text = "✦  ZIZI PANEL"
TitleLabel.Size = UDim2.new(1, 0, 1, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.TextColor3 = C.White
TitleLabel.TextSize = 13
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.ZIndex = 3
TitleLabel.Parent = Titlebar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Text = "×"
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -36, 0.5, -15)
CloseBtn.BackgroundColor3 = C.Surface
CloseBtn.BorderSizePixel = 0
CloseBtn.TextColor3 = C.Muted
CloseBtn.TextSize = 18
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.ZIndex = 3
CloseBtn.Parent = Titlebar
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)

-- HEADER
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 70)
Header.Position = UDim2.new(0, 0, 0, 44)
Header.BackgroundTransparency = 1
Header.ZIndex = 2
Header.Parent = Main

-- Avatar placeholder
local AvatarBox = Instance.new("Frame")
AvatarBox.Size = UDim2.new(0, 46, 0, 46)
AvatarBox.Position = UDim2.new(0, 16, 0.5, -23)
AvatarBox.BackgroundColor3 = C.Surface
AvatarBox.BorderSizePixel = 0
AvatarBox.ZIndex = 3
AvatarBox.Parent = Header
Instance.new("UICorner", AvatarBox).CornerRadius = UDim.new(1, 0)
local AvatarStroke = Instance.new("UIStroke", AvatarBox)
AvatarStroke.Color = C.Accent
AvatarStroke.Thickness = 2

local AvatarLetter = Instance.new("TextLabel")
AvatarLetter.Text = string.upper(string.sub(LocalPlayer.Name, 1, 1))
AvatarLetter.Size = UDim2.new(1, 0, 1, 0)
AvatarLetter.BackgroundTransparency = 1
AvatarLetter.TextColor3 = C.Accent
AvatarLetter.TextSize = 20
AvatarLetter.Font = Enum.Font.GothamBold
AvatarLetter.ZIndex = 4
AvatarLetter.Parent = AvatarBox

local UserName = Instance.new("TextLabel")
UserName.Text = LocalPlayer.Name
UserName.Position = UDim2.new(0, 72, 0, 14)
UserName.Size = UDim2.new(1, -80, 0, 22)
UserName.BackgroundTransparency = 1
UserName.TextColor3 = C.White
UserName.TextSize = 16
UserName.TextXAlignment = Enum.TextXAlignment.Left
UserName.Font = Enum.Font.GothamBold
UserName.ZIndex = 3
UserName.Parent = Header

local SubLabel = Instance.new("TextLabel")
SubLabel.Text = "v2.1 · Premium · Build 2026"
SubLabel.Position = UDim2.new(0, 72, 0, 38)
SubLabel.Size = UDim2.new(1, -80, 0, 16)
SubLabel.BackgroundTransparency = 1
SubLabel.TextColor3 = C.Muted
SubLabel.TextSize = 11
SubLabel.TextXAlignment = Enum.TextXAlignment.Left
SubLabel.Font = Enum.Font.Gotham
SubLabel.ZIndex = 3
SubLabel.Parent = Header

-- Séparateur
local Sep = Instance.new("Frame")
Sep.Size = UDim2.new(1, -32, 0, 1)
Sep.Position = UDim2.new(0, 16, 0, 114)
Sep.BackgroundColor3 = C.Border
Sep.BorderSizePixel = 0
Sep.BackgroundTransparency = 0.5
Sep.Parent = Main

-- TABBAR
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -32, 0, 36)
TabBar.Position = UDim2.new(0, 16, 0, 122)
TabBar.BackgroundColor3 = C.Surface
TabBar.BorderSizePixel = 0
TabBar.ZIndex = 2
TabBar.Parent = Main
Instance.new("UICorner", TabBar).CornerRadius = UDim.new(0, 10)

local TabLayout = Instance.new("UIListLayout")
TabLayout.FillDirection = Enum.FillDirection.Horizontal
TabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left
TabLayout.VerticalAlignment = Enum.VerticalAlignment.Center
TabLayout.Parent = TabBar

local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, -32, 1, -176)
ContentArea.Position = UDim2.new(0, 16, 0, 168)
ContentArea.BackgroundTransparency = 1
ContentArea.BorderSizePixel = 0
ContentArea.ClipsDescendants = true
ContentArea.Parent = Main

local TabNames = {"Player", "Game", "Troll", "ESP", "Other", "Credits"}
local TabPages = {}
local TabButtons = {}
local ActiveTab = "Player"

local function SwitchTab(name)
    ActiveTab = name
    for _, page in pairs(TabPages) do page.Visible = false end
    if TabPages[name] then TabPages[name].Visible = true end
    for n, btn in pairs(TabButtons) do
        if n == name then
            TweenService:Create(btn, TweenInfo.new(0.2), {
                BackgroundColor3 = C.Accent,
                TextColor3 = C.White,
                BackgroundTransparency = 0
            }):Play()
        else
            TweenService:Create(btn, TweenInfo.new(0.2), {
                BackgroundColor3 = C.Surface,
                TextColor3 = C.Muted,
                BackgroundTransparency = 1
            }):Play()
        end
    end
end

for _, name in ipairs(TabNames) do
    local btn = Instance.new("TextButton")
    btn.Text = name
    btn.Size = UDim2.new(0, (420 - 32) / #TabNames, 1, 0)
    btn.BackgroundColor3 = C.Surface
    btn.BackgroundTransparency = 1
    btn.TextColor3 = C.Muted
    btn.TextSize = 11
    btn.Font = Enum.Font.GothamBold
    btn.ZIndex = 3
    btn.Parent = TabBar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    TabButtons[name] = btn

    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 2
    page.ScrollBarImageColor3 = C.Accent
    page.Visible = false
    page.Parent = ContentArea
    TabPages[name] = page

    local pageLayout = Instance.new("UIListLayout")
    pageLayout.Padding = UDim.new(0, 8)
    pageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    pageLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    pageLayout.Parent = page

    local pagePad = Instance.new("UIPadding")
    pagePad.PaddingTop = UDim.new(0, 10)
    pagePad.PaddingLeft = UDim.new(0, 4)
    pagePad.PaddingRight = UDim.new(0, 4)
    pagePad.Parent = page

    pageLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        page.CanvasSize = UDim2.new(0, 0, 0, pageLayout.AbsoluteContentSize.Y + 20)
    end)

    btn.MouseButton1Click:Connect(function() SwitchTab(name) end)
end

SwitchTab("Player")

-- HELPERS
local function MakeCard(parent, order)
    local card = Instance.new("Frame")
    card.Size = UDim2.new(1, 0, 0, 0)
    card.AutomaticSize = Enum.AutomaticSize.Y
    card.BackgroundColor3 = C.Surface
    card.BorderSizePixel = 0
    card.LayoutOrder = order or 0
    card.Parent = parent
    Instance.new("UICorner", card).CornerRadius = UDim.new(0, 12)
    local pad = Instance.new("UIPadding", card)
    pad.PaddingTop = UDim.new(0, 10)
    pad.PaddingBottom = UDim.new(0, 10)
    pad.PaddingLeft = UDim.new(0, 12)
    pad.PaddingRight = UDim.new(0, 12)
    local layout = Instance.new("UIListLayout", card)
    layout.Padding = UDim.new(0, 8)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    return card
end

local function MakeLabel(parent, text, order)
    local lbl = Instance.new("TextLabel")
    lbl.Text = string.upper(text)
    lbl.Size = UDim2.new(1, 0, 0, 16)
    lbl.BackgroundTransparency = 1
    lbl.TextColor3 = C.Accent
    lbl.TextSize = 10
    lbl.Font = Enum.Font.GothamBold
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.LayoutOrder = order or 0
    lbl.Parent = parent
    return lbl
end

local function MakeSlider(parent, labelText, min, max, default, unit, callback, order)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 54)
    row.BackgroundTransparency = 1
    row.LayoutOrder = order or 0
    row.Parent = parent

    local topRow = Instance.new("Frame")
    topRow.Size = UDim2.new(1, 0, 0, 20)
    topRow.BackgroundTransparency = 1
    topRow.Parent = row

    local lbl = Instance.new("TextLabel")
    lbl.Text = labelText
    lbl.Size = UDim2.new(0.6, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.TextColor3 = C.White
    lbl.TextSize = 13
    lbl.Font = Enum.Font.GothamBold
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = topRow

    local valLbl = Instance.new("TextLabel")
    valLbl.Text = tostring(default) .. " " .. unit
    valLbl.Size = UDim2.new(0.4, 0, 1, 0)
    valLbl.Position = UDim2.new(0.6, 0, 0, 0)
    valLbl.BackgroundTransparency = 1
    valLbl.TextColor3 = C.AccentB
    valLbl.TextSize = 12
    valLbl.Font = Enum.Font.GothamBold
    valLbl.T
